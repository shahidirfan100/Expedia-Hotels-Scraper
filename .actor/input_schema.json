{
  "title": "Expedia Hotels Listings Scraper",
  "description": "Extract Expedia hotel listing cards from Hotel-Search results with prices, ratings, and property links.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Expedia Hotel-Search URL",
      "type": "string",
      "description": "Expedia Hotel-Search or Expedia destination URL. Messy, partial, or stale URLs are normalized automatically and missing common search params are healed at runtime.",
      "editor": "textfield",
      "prefill": "https://www.expedia.com/Hotel-Search?regionId=6139104&destination=London%2C%20United%20Kingdom%20%28LON-All%20Airports%29"
    },
    "results_wanted": {
      "title": "Maximum listings",
      "type": "integer",
      "description": "Maximum number of hotel listings to save.",
      "minimum": 1,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum page loads",
      "type": "integer",
      "description": "Maximum result-load rounds to attempt while scrolling/loading more.",
      "minimum": 1,
      "default": 8,
      "prefill": 8,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Residential proxy is strongly recommended for reliable extraction.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      }
    }
  },
  "required": ["startUrl"],
  "additionalProperties": false
}
